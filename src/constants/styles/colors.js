import {Dimensions, Platform, StatusBar} from 'react-native';

const {height, width} = Dimensions.get('window');

export default {
  theme: '#28AF6E',
  secondary: '#101E17',

  paywall_card: 'rgba(255, 255, 255, 0.08)',
};
